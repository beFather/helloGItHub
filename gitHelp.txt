git config --global user.name beFather
git  config user.name
git config --global user.email addalpa@gmail.com
git  config user.email
cat ~/.gitconfig
git config --list

git remote add orgin git@github.com:beFather/helloGItHub.git
git clone https://github.com/beFather/helloGItHub.git 
git pull 
git add -all 
git add <fileName>
git add . 
git commit -am "comment"
git push -u origin master

git 
    revert : 히스토리 백
    reset  : 히스토리 지움 

git push origin <branchName>
git branch -r  //서버 브랜치 목록
git branch -a
git checkout br1 //브랜치변경
git push origin br1 //브랜치 반영
-- br1에서 현재브랜치로 파일update
git checkout -p br1 gitHelp.txt
git checkout -t origin/<branchName>
