git config --global user.name beFather
git  config user.name
git config --global user.email addalpa@gmail.com
git  config user.email
cat ~/.gitconfig
git config --list

git remote add orgin git@github.com:beFather/helloGItHub.git
git clone https://github.com/beFather/helloGItHub.git 
git pull 
git add -all 
git add <fileName>
git add . 
git commit -am "comment"
git push -u origin master

git 
    revert : 히스토리 백
    reset  : 히스토리 지움 